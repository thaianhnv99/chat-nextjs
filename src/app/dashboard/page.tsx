import ButtonUI from "@/components/ui/ButtonUI";
import { db } from "@/lib/db";

interface PageProps {}
const Page = ({}: PageProps) => {
  // db.set("hello", "hello");
  return <>Dashboard: </>;
};

export default Page;
