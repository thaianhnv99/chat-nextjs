"use client";

import Box from "@mui/material/Box";
import Button from "@mui/material/Button";

interface ButtonProps {}
const ButtonUI = ({}: ButtonProps) => (
  <Box>
    <Button>Button MUI</Button>
  </Box>
);

export default ButtonUI;
